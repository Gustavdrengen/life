# BLOCKED

> Agent → programmer channel. Used only when the agent cannot proceed without
> external help (credentials, environment, vision-level decisions the agent
> cannot infer). Format for each entry: `[YYYY-MM-DD] what is blocked` with
> `Tried`, `Needed`, `Impact` sub-bullets. Add when discovered, update on
> status change, **remove when cleared**.
